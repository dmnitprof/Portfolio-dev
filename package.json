{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite  --host --open",
    "build": "vite build",
    "serve": "vite preview --host --open"
  },
  "devDependencies": {
    "sass": "^1.35.1",
    "vite": "^2.3.7"
  }
}
